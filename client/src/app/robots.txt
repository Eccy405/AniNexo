import { NextResponse } from 'next/server';

// robots.txt endpoint
export async function GET() {
  const robots = `User-agent: *
Allow: /
Sitemap: https://aninexo.com/sitemap.xml`
`;
  return new NextResponse(robots, {
    headers: {
      'Content-Type': 'text/plain',
    },
  });
}
